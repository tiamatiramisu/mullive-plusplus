# mullive-plusplus STATUS

현재 버전: `v0.22.0` / GitHub: `tiamatiramisu/mullive-plusplus` / Greasy Fork: [593484](https://greasyfork.org/ko/scripts/593484)

## 스테이지

- [x] Stage 0 — 스캐폴딩 + 릴리스 파이프라인 + CSP 스모크 테스트 `v0.1.0`
- [x] Stage 2 — 채팅창 너비 조절 (F1) `v0.2.0`
- [x] Stage 3 — 절대 위치 엔진 + 다중 채팅 + 열/사이드 자동 전환 `v0.3.1`
- [x] Stage 5 — 드래그 위치 교환 `v0.4.0`
- [x] Stage 4 — 수동 m×n 격자 + 갭 제거 + 리사이저 그립 `v0.5.0`

Stage 1(업스트림 분석)은 `SPEC.md`로 완료.

### Stage 0 상세

- [x] esbuild 번들 + `meta.js` 배너 · 태그→`@version` 주입
- [x] `jsconfig.json` checkJs 타입체크
- [x] GitHub repo 생성 · SSH 원격 · 최초 push
- [x] 태그 릴리스 워크플로우 (`v0.1.0` 성공, 에셋 `mullive-plusplus.user.js` 6044 B)
- [x] Greasy Fork `release` 웹훅 등록 (hook id `671923702`, ping → 200 OK)
- [x] 페이지 훅 표 실사이트 검증
- [x] CSP 주입 경로 실측 → `style.js` 판정 로직 수정
- [x] Greasy Fork 스크립트 등록 (id `593484`, 언어 JavaScript, 동기화 `자동`)
- [x] Violentmonkey 설치 후 스모크 테스트

## 다음 할 일

목표 1~5 모두 구현 완료. 이후는 실사용 피드백 반영.

## 알려진 이슈

- **자동 배치의 커버리지가 낮다.** 현재 엔진은 "16:9 타일을 가장 크게" 만 기준으로 삼는다.
  1920×1080 + 채팅 356px에서 3분할이면 2×2(779×438)를 골라 가용 영역의 60.8%만 덮는다.
  타일을 키우는 것과 영역을 채우는 것이 다른 목표라서 생기는 문제이며 Stage 4에서 다룬다.
- **배경 탭에서는 영상이 렌더되지 않는다.** 자동 검증은 `visibilityState: hidden` 상태로 돌기 때문에
  스크린샷의 영상 영역이 검게 나온다. 레이아웃 수치는 정확하므로 기하 검증에는 영향이 없으나,
  화질·재생 관련은 사용자 육안 확인이 필요하다.
- **flex 줄바꿈은 계산된 열 수를 강제하지 않는다.** 타일 폭만 지정하므로 실제 줄바꿈은 브라우저가 정한다.
  현재까지 확인된 케이스에서는 계산과 일치하지만, 원리상 어긋날 수 있다. Stage 4에서 CSS Grid로 전환하며 해소한다.
- 페이지의 `message` 핸들러는 `e.source`를 **영상** iframe 목록에서만 찾는다.
  채팅 프레임이 보내는 `PonReady`는 `idx === -1`이 되어 `iframes[-1].name`에서 예외가 난다.
  현행(채팅 1개)에서도 발생하는 문제이며, Stage 3에서 채팅 iframe이 늘 때 빈도를 관측할 것.

## 검증 로그

### 2026-08-30 — v0.22.0 Shift+우클릭 · LIFO 전환 · 분할 규칙 확정

**설정 대신 Shift.** `chatRightClick` 드롭다운을 없애고 우클릭 = 전환, Shift+우클릭 = 칸 추가/제거로
갈랐다. 에이전트가 `contextmenu` 에서 `e.shiftKey` 를 같이 보고한다.
드롭다운이 없어졌으니 탭 안내문이 Shift 의 유일한 단서다 — 문구에 적어 넣었다.
패널 실측: 채팅 탭 `select` 0개, 안내문에 Shift 포함.

**우클릭이 분할을 없애지 않는다.** 전환은 **마지막 칸만** 갈아끼운다(LIFO).
호버 미리보기가 노리는 칸과 같은 자리라, 넘겨보다 우클릭하면 보던 그 자리에 확정된다.

**마스터 채팅 장부를 솔로와 맞췄다.** `masterChat` + `masterChatAuto` 가
`audio.js` 의 `masterAutoPinned` 와 같은 일을 한다. 원래 켜 두었던 채팅이면 마스터를 풀 때
맨 뒤(가장 작은 칸)로 밀고, 우리가 켠 것이면 같이 없앤다.
마스터인 동안 직접 Shift+우클릭하면 사용자 것이 된다.

**분할 규칙 최종.** 한 번에 계산하던 것을 **한 칸씩 늘려 가는** 방식으로 바꿨다.
칸을 더할 때마다 **가장 큰 칸**을 반으로 자르되, **면적이 2% 이내로 비슷하면 마지막에 생긴 칸**을
자른다. 이 tie-break 가 핵심이다 — 없으면 반올림 1px 차이로 첫 칸이 먼저 잘려 메인이 무너진다.
있으면 첫 칸은 혼자 눈에 띄게 클 때만 잘리고, 그 시점이 나머지가 모두 한 번씩 쪼개진 뒤라
**4칸에서 저절로 균등 사분할**이 된다.

node 로 2개 폭 × 1~6칸: **겹침 0, 이탈 0, 빈틈 0, 칸 수 일치.**

| 채팅 패널 | 2칸 | 3칸 | 4칸 |
|---|---|---|---|
| 641 | 321×889 + 320×889 | 메인 321×889 유지 + 320×445·444 | 321×445 · 320×445 · 320×444 · 321×444 (균등) |
| 350 | 350×445 + 444 | 메인 350×445 유지 + 350×222 ×2 | 350×223 + 222 ×3 (균등) |

**자동화 한계.** 휠클릭(마스터)과 일부 우클릭 경로는 교차 출처 iframe 안에서 나야 하는데
자동화 도구가 가운데 클릭을 못 만들고, 우클릭도 작은 스택 타일에서는 프레임까지 닿지 않는 것을
`message` 리스너로 확인했다(로그 0건). 사용자 조작으로 검증했다.

### 2026-08-30 — v0.21.0 채팅 칸 나누기

**우클릭 두 모드.** `chatRightClick` 으로 `채팅 전환`(기존)과 `채팅 추가/제거`(신규)를 고른다.
설정 항목은 `inline` 플래그로 `라벨 [드롭다운]` 한 줄에 들어간다.

**분할 규칙을 두 번 고쳤다. 둘 다 실측에서 틀린 걸 보고 고친 것이다.**

1. 처음엔 타일링 WM 그대로 **긴 변**을 잘랐다. 745×889 패널에서 위아래로 쌓였다.
   채팅은 지나간 줄 수가 전부라 높이가 곧 쓸모고, 폭은 최소치까지 깎아도 읽힌다.
   → **열(세로선)을 먼저** 자르고, 최소 폭 240에 걸릴 때만 가로로 눕힌다.
2. 그다음엔 칸이 늘 때마다 전부 다시 비례 배분해서 **처음 보던 채팅이 같이 쪼그라들었다.**
   → **첫 칸을 메인으로 떼어 놓고** 나머지끼리만 나눈다(dwm master + stack).
   stack 쪽은 BSP의 반씩 자르기 대신 칸 수 비례로 고르게 나눈다. 채팅끼리는 대등하다.

메인 몫을 지키면 다 못 담는 좁은 패널에서는 메인을 포기하고 전부 고르게 나눈다.
칸 하한 240×200, 한 칸뿐일 때는 따지지 않는다.

node 로 3개 폭 × 칸 수 1~6 을 돌려 **겹침 0, 영역 이탈 0, 빈틈 0, 칸 수 일치** 확인:

| 채팅 패널 | 2칸 | 3칸 | 4칸 | 5칸 |
|---|---|---|---|---|
| 745 | 메인 373×889 유지 | 메인 유지 + 372×445·444 | 메인 유지 + 372×297·296 | 메인 유지 + 372×223·222 |
| 350 | 350×445 + 444 | 350×445 + 222 | 350×223 ×4 (메인 포기) | 안 들어감 |

**실제 화면 확인** (1920×889, 채팅 641, 3칸):
`mlpp-chat-2` 가 1279,0 **321×889**, `mlpp-chat-3` 가 1600,0 320×445, `mlpp-chat-1` 이 1600,445 320×444.
메인이 안 쪼그라들고 나머지 둘만 옆에 쌓였다.

**호버는 마지막 칸만 바꾼다.** 우클릭 설정과 무관하다. 스트림 0에 호버하니
`chat` 이 `2+3+1` → `2+3+0` 으로 바뀌고 **사각형은 그대로**, `panes` 는 `[2,3,1]` 로 남았다.
첫 칸과 칸 수가 안 흔들리니 넘겨보는 동안 배치가 튀지 않는다.

**마스터 채팅은 첫 칸에.** `showPane` 이 앞에 꽂고, 자리가 없으면 맨 뒤를 밀어낸다.
휠클릭은 교차 출처 iframe 안에서 나야 하는데 자동화 도구에 가운데 클릭 액션이 없어 끝까지 몰지 못했다.

### 2026-08-30 — v0.20.0 마스터 스택 아래 배치 + 패널 묶음

**`bottom` 스택 신설.** `masterStackLayout` 에 `placement` 인자를 넣었다.
`bottom` 은 스택이 가로줄이라 **가용 폭**이 슬레이브 높이의 상한을 정한다(`right` 는 화면 높이가 폭을 정한다).
어느 쪽이든 스택이 먼저 크기를 정하고 마스터가 남은 자리를 16:9로 채우는 구조는 같다.

순수 함수라 node 로 18개 조합(방송 수 2·3·4·6·9 × 1920×1080 / 1440×900 / 1280×720 / 800×600 / 900×1600)을
돌려 **겹침 0, 영역 이탈 0, 비율 전부 16:9(±0.03)** 확인. 커버리지 실측:

| 1920×1080 · 채팅 350 | 마스터 | 커버 |
|---|---|---|
| 4방송 `bottom` | 1440×810 | 91.7% |
| 4방송 `right` | 1178×662 | 61.3% |
| 6방송 `bottom` | 1570×883 | 98.0% |
| 6방송 `right` | 1186×667 | 71.1% |

가로 화면에서 `right` 가 손해인 이유는 세로 스택이 화면 높이에 조여 슬레이브가 작아지고,
그만큼 남는 폭을 마스터가 16:9 때문에 쓰지 못하기 때문이다. 기본을 `bottom` 으로 잡았다.
세로 화면(900×1600)은 둘 다 나쁘다(28.9% / 13.0%) — 마스터 앤 스택 자체가 가로 화면 기능이다.

**설정이 배치까지 닿는지** — 패널에서 옵션을 바꾸며 `data-mlpp-layout` 의 `stack` 을 읽어
`bottom` → `right` → `bottom` 을 확인했다.

**한계.** 마스터 진입 자체는 교차 출처 iframe 안의 **휠클릭**이 필요한데 자동화 도구에
가운데 클릭 액션이 없어 끝까지 몰지 못했다. 기하는 위 단위 검증으로, 배선은 `stack=` 진단으로 대신했다.

**패널 묶음** — `group` 을 준 이웃 항목이 위아래 선 상자 안에 가로로 들어간다.
실측: 상자 폭 310px 안에서 `열 수`/`행 수` 가 각각 148px, 상하 테두리 1px, 설명은 상자 아래 한 줄.
안내문 앞머리는 `<b>` 로 굵게(`마스터 지정: …`).

### 2026-08-30 — v0.19.0 호버 미리보기 토글 + 사운드 탭 정리

**호버 토글** — 채팅·사운드 탭 맨 위에 "호버로 미리 확인"을 각각 넣었다(기본 켬).
게이트는 값을 읽는 지점 한 곳씩이다. 채팅은 `activeChat()` 에서
`preview >= 0 && chatHoverPreview !== 0` 일 때만 미리보기를 쓰고, 사운드는 `active()` 에서
`hovered` 를 솔로 집합에 넣을지 결정한다. 끄는 순간 `settings.onChange` 가
`schedule()`/`apply()` 를 부르므로 마우스를 움직이지 않아도 즉시 되돌아간다.
패널 실측: 두 탭 모두 안내문 바로 아래 첫 줄에 `ON` 으로 렌더.

**하위 설정 들여쓰기** — 스키마에 `indent: true` 를 두면 `.mlpp-sub` 로 12px 들여쓰고
왼쪽에 세로줄을 긋는다. `실제 소리에 반응` 이 `선택된 영상 시각화` 아래로 붙었다.

**`glowFromAudio` 기본 켬 — 비용 실측.** 페이지에서 실제 그래프를 만들어 측정했다.
`fftSize` 256 + 256칸 피크 루프 20000회 → **1회 0.0015ms**.
4방송 60fps 환산 **코어 1개의 0.037%**. 같은 시점 부모 탭 프레임 간격 중앙값 16.6ms / p95 17.0ms.
교차 출처 프레임은 렌더러 프로세스가 따로라 부모 탭에는 영향이 잡히지도 않는다.

**멈춘 `AudioContext` 가드.** 기본을 켜면서 생긴 진짜 위험은 CPU가 아니라 무음이다.
`createMediaElementSource` 는 오디오를 그래프로 돌려버리고 **되돌릴 수 없어서**,
자동재생 정책으로 컨텍스트가 `suspended` 로 만들어지면 그 방송은 통째로 소리가 사라진다.
에이전트가 `pointerdown`·`keydown` 마다 `resume()` 하고 `statechange` 로 상태를 보고하도록 했다.
`analysers` 집계도 "만들어졌다"에서 **"`state === 'running'` 이다"**로 바꿔 진단에서 구분된다.
실측 `analysers=[2,0,1,3]` — 4개 전부 running.

### 2026-08-29 — Stage 0

**릴리스 파이프라인** — `v0.1.0` 태그 push → Actions 성공 → 릴리스 에셋 생성.
`https://github.com/tiamatiramisu/mullive-plusplus/releases/latest/download/mullive-plusplus.user.js`
→ HTTP 200, 배포된 파일의 `@version` = `0.1.0` (태그값 주입 확인).
웹훅 배달 → Greasy Fork 전부 `200 OK` (`ping`, `release.created/published/released/edited/deleted`).

**개명** — 공식 확장 "Mul.Live Plus"와의 혼동을 피해 repo·산출물·콘솔 태그를 `mullive-plusplus`로 변경.
repo 이름을 바꿔도 웹훅(id `671923702`)은 그대로 유지됨을 확인. Greasy Fork 등록 전이라
기존 `v0.1.0` 릴리스·태그를 삭제하고 새 이름으로 다시 잘랐다.

**페이지 훅** — 실제 Chrome, `https://mul.live/khm11903/phonics1/ecvhao` (SOOP 3개), 뷰포트 560×862:

| 항목 | 결과 |
|---|---|
| `#streams` `#chat-container` `#chat` `#chat-select` `#chat-toggle` | 전부 존재 |
| `#chat-container` 계산된 width | `350px` (하드코딩 확인) |
| 영상 iframe `name` | `khm11903` `phonics1` `ecvhao` — 스트림 id와 일치 |
| 영상 iframe 인라인 style | `width:202px; height:113px; flex-grow:0` — 페이지 `adjustLayout()`이 인라인으로 쓰는 것 확인 |
| `#streams` 계산된 display | `flex` / `flex-wrap: wrap` |
| `#chat-select` 옵션 | SOOP 3개 + `about:blank`(숨기기). **`disabled` 없음 → Mul.Live Plus 확장 활성** |
| 페이지 전역 함수 | `adjustLayout`, `setName` 모두 `function` |

**CSP 주입 경로** — 같은 탭, 페이지 컨텍스트에서 직접 시도:

| 경로 | 결과 |
|---|---|
| `<style>` 요소 삽입 | **차단**. `securitypolicyviolation` (`style-src-elem`, blocked=`inline`), 외곽선 미적용 |
| `adoptedStyleSheets` + `replaceSync` | **통과**. 위반 없음, 외곽선 정상 적용 |

→ `style.js`의 성공 판정을 `isConnected`(막힌 요소도 true)에서 **센티넬 커스텀 속성 계산값**으로 변경.

**미검증** — `GM_addStyle` 경로는 유저스크립트 매니저가 필요해 아직 확인 못 함.

### 2026-08-29 — Greasy Fork 등록

동기화 URL `https://github.com/tiamatiramisu/mullive-plusplus/releases/latest/download/mullive-plusplus.user.js`
로 가져오기 완료. 스크립트 id `593484`, 표시 버전 `0.1.0`, 라이선스 MIT.
릴리스 에셋 경로에서 정상적으로 코드를 당겨왔음을 확인.

### 2026-08-29 — Stage 2 레이아웃 계산 점검 (node, 브라우저 없이)

`computeGrid(n, availW, availH, gap=4)` 결과. 모든 케이스에서 가용 영역을 넘지 않음.

| 상황 | 배치 | 타일 | 차지/가용 | 덮음 |
|---|---|---|---|---|
| 1920×1080, 채팅 356, n=2 | 1×2 | 954×537 | 954×1078 / 1562×1078 | 60.8% |
| 1920×1080, 채팅 356, n=3 | 2×2 | 779×438 | 1562×880 | 60.8% |
| 1920×1080, 채팅 356, n=4 | 2×2 | 779×438 | 1562×880 | 81.1% |
| 1920×1080, 채팅 356, n=6 | 2×3 | 632×356 | 1268×1076 | 80.2% |
| 1920×1080, 채팅 닫힘, n=3 | 2×2 | 954×537 | 1912×1078 | 74.3% |
| 1920×1080, 채팅 806, n=3 | 1×3 | 632×356 | 632×1076 | 56.3% |
| 1080×1920(세로), 채팅 356, n=4 | 1×4 | 722×406 | 722×1636 | 84.7% |

경계값: `n=0` → `null`, 음수 폭 → `null`, 20×20 영역 → `8×4` 타일(비정상 종료 없음).

### 2026-08-29 — Stage 2 브라우저 검증 (Violentmonkey, 실제 Chrome)

`https://mul.live/khm11903/phonics1/ecvhao` (SOOP 3개), 뷰포트 960×1458.

**주입 경로** — `data-mlpp-style="cssom"`, `adoptedStyleSheets` 1개, 고아 `<style>` 0개.
GM_addStyle 경로도 스타일 자체는 적용되지만(차단이 아니라 보고만) 삽입·갱신마다
`style-src-elem` 위반을 남긴다. 레이아웃은 리사이즈마다 갱신되므로 CSSOM을 1순위로 바꿨다.

**발견해 고친 버그 3건**

| 증상 | 원인 | 수정 |
|---|---|---|
| 채팅 패널이 화면 밖으로 밀림 (scrollWidth 1102 > 960) | 첫 계산 시점의 `#chat` src가 `about:blank`이라 채팅 폭 0으로 계산. SOOP은 확장이 나중에 src를 채운다 | 재계산이 실제로 발화하도록 수정 + `#streams`에 `min-width: 0` |
| src가 바뀌어도 재계산이 안 됨 | 재계산을 `requestAnimationFrame`으로 예약했는데 배경 탭에서는 rAF가 발화하지 않음 | `setTimeout(0)`으로 교체 (throttle될 뿐 반드시 발화) |
| 저장된 폭이 하한 240px로 눌린 뒤 창을 넓혀도 복구 안 됨 | 클램프를 저장값에 파괴적으로 적용 | 사용자 선택값(`preferred`)과 적용값(`effective()`)을 분리 |

**드래그** — 포인터 캡처 의존을 걷어내고 문서 레벨 리스너 + 실드 구조로 변경(캡처는 창 밖 대응 보강용).

| 단계 | 채팅 폭 | 타일 | 실드 | scrollWidth |
|---|---|---|---|---|
| 드래그 전 | 240px | 712×400 | 없음 | 960 |
| pointerdown | 240px | 712×400 | 있음 | 960 |
| pointermove → 500 | 500px | 452×254 | 있음 | 960 |
| pointerup | 500px | 452×254 | 제거됨 | 960 |

타일 452×254는 계산 기대값과 정확히 일치.

**수용 기준**

| 항목 | 결과 |
|---|---|
| 새로고침 후 폭 유지 | 500px / 452×254 |
| 채팅 닫기 | 패널·리사이저 `display:none`, 타일 856×482(전체 폭) |
| 다시 열기 | 500px 복원 |
| 더블클릭 리셋 | 350px / 602×338 |
| 오버플로우 | 전 구간 scrollWidth = 뷰포트(960) |
| 타일 배치 | `1,218` `1,560` `1,902` — 338×3+8=1022가 1458에 정확히 수직 중앙 정렬 |

### 2026-08-29 — Greasy Fork 릴리스 웹훅이 동작하지 않던 원인

`v0.2.0` 릴리스 후 웹훅은 200으로 배달됐으나 Greasy Fork는 `0.1.0`에 머물렀다.
배달 응답 본문:

```
Could not pull contents from git: fatal: path 'mullive-plusplus.user.js' does not exist in 'v0.2.0'
```

greasyfork `lib/github.rb`의 `info_from_release_event` → `file_from_root_for_url`은
동기화 URL `.../releases/latest/download/mullive-plusplus.user.js` 에서 `latest/`와 `download/`를
떼어내 **저장소 루트 기준 경로** `mullive-plusplus.user.js` 를 만들고,
`process_webhook_changes`가 `Git.get_contents(repo_url, { 그 경로 => 태그 })`로 꺼낸다.
**릴리스 에셋은 읽지 않는다.** 도움말의 "에셋" 설명은 최초 가져오기·주기적 동기화(HTTP 페치)에만 해당한다.

대응: 빌드 산출물을 저장소 루트에 커밋하고, 그 커밋에 태그를 단다(`release.mjs`).
워크플로우는 소스에서 다시 빌드해 커밋된 산출물과 `diff`로 대조하므로,
산출물을 갱신하지 않고 태그만 밀면 릴리스가 실패한다.

**검증** — `npm run release -- 0.2.1` → Actions 성공 → 웹훅 배달 응답:

```json
{"updated_scripts":["https://api.greasyfork.org/ko/scripts/593484-..."],"updated_failed":[]}
```

Greasy Fork 버전 이력에 `v0.2.1` 등재 확인. 파이프라인 끝단까지 동작.

### 2026-08-30 — 설계 변경: 열 모드 도입

사용자 요구: 가로 화면에서 영상을 1행으로 놓고 **그 아래에 각 방송의 채팅을 동시에** 보여준다.
세로 화면이면 영상을 세로로 쌓고 채팅은 하나만 둔다.

**구조적 함의** — "영상 위 / 그 아래 자기 채팅"을 만들려면 열 단위로 DOM을 묶어야 하는데
iframe을 DOM에서 옮기면 리로드된다. 그래서 배치를 전부 **절대 위치**로 바꿨다.
flex 기반이던 Stage 2 구현을 대체하며, 부수적으로 드래그 스왑이 좌표 교환만으로 가능해진다.

**전환 규칙** — `가로(W >= H)` **그리고** `열 폭 >= 400px`.
1920×1080이면 3·4분할이 열 모드, 5분할부터 사이드. 세로 화면은 항상 사이드.

**선행 실험 — SOOP 채팅 다중 표시 가능 여부**

열 모드의 전제였다. 확장(`soop.js`)이 `window.parent[플레이어이름]`으로 자기 방송을 찾으므로
프레임이 여러 개일 때 깨질 수 있었다. 실제로 채팅 iframe을 추가로 띄워 확인한 결과
**두 채팅이 각각 완전히 붙었다**(헤더·메시지·입력창까지). 열 모드 성립.

단, 방송 6개가 도는 탭에 채팅 6개를 얹자 **렌더러가 멈췄다**(스크린샷 CDP 타임아웃).
무제한 유지는 위험하므로 `chatLimit` 설정을 뒀다(기본 0 = 무제한, 사용자 결정).

### 2026-08-30 — Stage 3 브라우저 검증 (사이드 모드)

`https://mul.live/khm11903/phonics1/lshooooo`, 뷰포트 960×1423.

| 항목 | 결과 |
|---|---|
| 주입 경로 | `cssom` |
| 영상 | 절대 위치 3개, `0,331` `0,586` `0,841` 447×251 — 수직 중앙 정렬 정확 |
| 페이지 `#chat` | `about:blank`로 고정됨 |
| `#chat-container` | `display:none` |
| `#chat-select` | `#mlpp-chats`로 이관, 페이지 참조 유지 |
| 오버플로 | 없음 (960 = 뷰포트) |

**채팅 전환 유지** — 0 → 1 → 2 → 0 순으로 전환:

| 단계 | 프레임 상태 |
|---|---|
| 초기 | `chat-0 visible` |
| 1번 | `chat-0 hidden(507×1395)`, `chat-1 visible` |
| 2번 | `chat-0 hidden`, `chat-1 hidden`, `chat-2 visible` |
| 0번 복귀 | `chat-0 visible`, 나머지 hidden |

0번 프레임에 심어둔 `dataset` 마크가 복귀 후에도 남아 있었다 → **요소가 재생성되지 않았다 = 리로드 없음**.
숨은 채팅도 507×1395 크기를 유지해 뒤에서 계속 내려간다.

**채팅 토글** — 끄면 영상이 447×251 → 837×471로 가용 영역 전체를 쓰고, 리사이저·select가 숨는다.
켜면 정확히 복원되며 채팅도 살아 있던 것이 그대로 보인다. 토글 아이콘은 페이지가 `#chat`의 src로 판단하지만
우리는 `#chat`을 비워두므로 우리 상태로 직접 구동한다.

**미검증** — 열 모드 렌더링. 사용자 모니터가 960×1600 **세로**(dpr 1.25, 물리 1200×2000)이고
창이 최대화라 `resize_window`가 무시되어 가로 뷰포트를 만들 수 없었다.
기하 계산 자체는 node로 전 시나리오 확인 완료.

### 2026-08-30 — v0.3.0 릴리스

열 모드 렌더 검증 전에 릴리스했다(사용자가 다른 기기에서 확인해야 하는 상황).
Actions 성공, 에셋 20645 B, 웹훅 배달 `updated_scripts`에 스크립트 URL — Greasy Fork 동기화 완료.

### 2026-08-30 — 열 모드에서 채팅이 방에 입장하지 못하던 버그

증상: 열 모드에서 채팅 3개 중 보통 2개가 "방송에 입장하였습니다"를 띄우지 못하고 빈 UI만 남았다.

**원인** — SOOP 채팅 프레임은 혼자 방에 들어가지 못한다.
확장(`soop.js`)이 `window.opener = window.parent[플레이어이름]`으로 플레이어를 이어주고,
채팅은 그 플레이어에게서 방송 정보를 받아 입장한다.
열 모드는 채팅 3개를 부팅 즉시 한꺼번에 만들었는데, 그 시점의 플레이어는 아직 `Pload`도 받기 전이다.

**확증 실험** — 페이지가 다 뜬 뒤 3개 중 2개만 `f.src = f.src`로 재로드했다.
재로드한 둘은 정상 입장(하나는 실시간 메시지까지), 대조군으로 놔둔 하나는 계속 비어 있었다.

**수정** — `src/ready.js`. 플레이어에서 오는 `PupdateBroadInfo`(방송 정보 도착 = 가장 확실) 또는
`PonReady` + 2초 정착 시간을 기다린 뒤에 채팅 프레임을 만든다.
신호가 15초 안에 오지 않으면 포기하고 그냥 만든다(경고 로그).
SOOP 외 플랫폼 채팅은 플레이어와 무관하므로 기다리지 않는다.
리스너는 훅을 찾기 전에 신호가 올 수 있어 스크립트 최상단에서 건다.

**검증** — 1920×889 가로, SOOP 3분할.
채팅 프레임 생성 시각이 부팅 즉시에서 약 5.2초로 미뤄졌고, **세 채팅 모두 입장 성공**.
열 모드 배치도 확인: 영상 `0,0` `641,0` `1282,0` 637×358, 채팅 `0,362` `641,362` `1282,362` 637×527.

### 2026-08-30 — 채팅 로딩이 15초 걸리던 문제

v0.3.1은 채팅이 뜨긴 했지만 **15초**가 걸렸다. 리소스 타이밍으로 재보니 원인이 둘이었다.

**1. 준비 신호를 한 번도 받지 못했다.** `ready.js`의 `if (!(e.source instanceof Window)) return;`
가드가 모든 메시지를 걸러내고 있었다. **교차 출처 프레임의 window는 `instanceof Window`가 false다**
(프로토타입 체인이 막혀 있다). 그래서 항상 15초 give-up 폴백으로만 채팅이 만들어졌다.
페이지 원본이 `e.source === f.contentWindow`로 비교하는 이유이기도 하다. `===`와 `Set` 멤버십은 정상이다.

```
교차출처 contentWindow instanceof Window  →  false
=== 비교 / Set 멤버십                     →  정상
```

**2. `PonReady` 즉시는 너무 이르다.** 가드를 고치자 채팅이 약 700ms에 만들어졌는데,
3개 중 1개만 입장했다. 페이지가 `PonReady`에 대한 응답으로 보내는 `Pload`를
플레이어가 처리할 시간이 필요하다. 또 채팅 3개를 같은 틱에 띄우면
플레이어들이 동시에 재생을 시작하는 시점과 겹쳐 실패한다.

**수정** — `PonReady` + 0.5초 정착, 그리고 생성 큐를 두고 `chatStagger`(기본 800ms) 간격으로 하나씩 만든다.
give-up은 10초로 낮췄다. 로딩 중에는 자리 표시자("… 채팅 불러오는 중")를 깔아
검은 화면이 고장인지 로딩인지 구분되게 했다.

**측정** (1920×889, 배경 탭)

| | 채팅 생성 시각 | 완료 |
|---|---|---|
| v0.3.1 (신호 미수신 → 폴백) | 15244ms (3개 동시) | 15.8s |
| 수정 후 3분할 | 2023 / 3015 / 4023ms | 4.2s |
| 수정 후 4분할 | 1613 / 2418 / 3224 / 4033ms | 4.7s |

**검증** — 3분할·4분할 모두 채팅 전부 입장 성공.
4분할 포그라운드 스크린샷에서 영상 4개(477×268)가 한 줄, 그 아래 채팅 4개에 실시간 메시지가 흐르는 것을 확인.

### 2026-08-30 — Stage 5: 드래그 위치 교환

**조작 방식** — Alt(Option)를 누른 동안만 오버레이가 나타난다.
상시 핸들을 얹는 안은 버렸다. SOOP 플레이어가 상단·모서리에 자체 UI를 두기 때문에
가리거나 클릭을 먹는다. 평소 오버레이는 `pointer-events: none`이라 방해가 전혀 없다.

**구현** — 바꾸는 것은 슬롯(화면 자리) → 스트림(방송) 대응 배열뿐이다.
`#streams iframe:nth-child(order[slot] + 1)` 에 슬롯의 좌표를 준다.
DOM을 건드리지 않으므로 재생이 끊기지 않는다. 열 모드에서는 채팅도 같은 대응을 따라간다.

**검증** (1920×895, SOOP 3분할, 열 모드)

| 항목 | 결과 |
|---|---|
| 교환 전 | `khm11903@0  phonics1@641  ecvhao@1282` |
| 슬롯 0 ↔ 2 드래그 후 | `khm11903@1282  phonics1@641  ecvhao@0` |
| 채팅 동반 이동 | `chat-2@0  chat-1@641  chat-0@1282` |
| **리로드 여부** | iframe에 심은 dataset 마크 전부 생존 → **재생 안 끊김** |
| Alt on/off | 오버레이 표시·숨김, 드래그 실드 생성·제거 정상 |
| 새로고침 후 | 교환된 순서 복원 |
| 되돌리기 | 원래 순서로 복귀 |

오버레이는 3px 테두리 + 라벨 pill(어두운 배경)로, 밝은 영상 위에서도 읽힌다.

### 2026-08-30 — Stage 4: 수동 격자 · 갭 제거 · 리사이저 그립

**갭** — `tileGap` 기본값을 4에서 0으로. 영상이 딱 붙는다.
1920×888 3분할 실측: 영상 `0,0` `640,0` `1280,0` 각 640×360, 사이 간격 0, 전체 폭 1920 사용.
채팅도 y=360에 바로 붙는다.

**리사이저** — 세로 전체를 덮는 막대는 영상과 채팅 사이에 경계선을 만들어 거슬렸다.
잡는 영역(6px 폭 × 세로 전체)은 그대로 두고, 보이는 것만 가운데의 짧은 그립으로 바꿨다.
`::after`로 4×56px, `rgba(255,255,255,0.1)`, 올리면 80px에 `0.4`로 진해진다.
실측 확인: 리사이저 배경 `rgba(0,0,0,0)`, 테두리 0px, 그립 56px/4px/10%.

**수동 격자** — `gridCols`/`gridRows`. `gridWith(cols, rows, ...)`를 뽑아내
`computeGrid`가 그것을 열 수마다 돌려 최선을 고르는 구조로 정리했다.

| 설정 | 1920×900, gap 0 결과 |
|---|---|
| 자동 4분할 | 2×2, 타일 782×439 |
| 수동 4열 4분할 | 1×4, 타일 391×219 |
| 수동 1열 3분할 | 3×1, 타일 533×300 |
| 수동 2열 5분할 | 3행으로 자동 증가, 마지막 행 1개 가운데 정렬 |
| 수동 2열×3행 4분할 | 3행 확보(빈 칸 1개), 타일 533×300 |

**주의점 기록** — JSDoc 블록 안에서 숫자 바로 뒤에 한글을 붙이면
`TS1351: An identifier or keyword cannot immediately follow a numeric literal`로 타입체크가 죽는다.
`0이면`, `3단` 같은 표현이 해당된다. 워크스페이스 `CLAUDE.md`에 규칙으로 적어뒀다.

### 2026-08-30 — 리사이저가 차지하던 세로 스트립 제거

핸들로 바꿔도 6px 컬럼이 레이아웃에 남아 있으면 낭비는 그대로였다.
`sideLayout`에서 리사이저 폭을 가용 폭 계산에서 빼고, 리사이저 사각형을 채팅 **왼쪽 끝에 겹치도록** 옮겼다.
드래그 계산도 그립 가운데가 커서를 따라오게 부호를 바꿨고, `#chat-select`는 그 폭만큼 오른쪽으로 밀었다.

실측 (960×1458 세로, 사이드 모드, 채팅 576):

| 항목 | 결과 |
|---|---|
| 영상 영역 오른쪽 끝 / 채팅 시작 | 384 / 384 — 갭 없음 |
| 리사이저 | `384~390`, 채팅(`384~960`) 안쪽에 겹침 |
| `elementFromPoint(387, 화면중앙)` | `mlpp-resizer` — 채팅 iframe에 가리지 않고 잡힌다 |
| `#chat-select` | 392부터 시작 — 잡는 영역을 가리지 않음 |

### 2026-08-30 — Stage 6: 솔로 / 음소거

**선행 조사** — 플레이어 번들(`LivePlayer.js`)을 받아 프로토콜을 뜯었다.
외부 명령은 여섯 개뿐이고 음소거가 없다: `Pload` `Pplay` `Ppause` `PtoggleChat` `PsetDarkMode` `PisPlayerWatching`.
디스패치는 화이트리스트가 아니지만(`"function"==typeof this[t.data.cmd]`) 그 클래스에 음소거 메서드가 없다.

**해법** — 유저스크립트를 플레이어 iframe 안에서도 돌린다(`@match play.sooplive.*`).
프레임 안 에이전트가 `#btn_sound`를 직접 누르고, 호버·가운데 클릭을 부모에 보고한다.
`#btn_sound`는 토글이지만 `mute` 클래스로 상태를 읽을 수 있어 절대 상태를 맞출 수 있다.

| 클릭 | 버튼 class | 툴팁 | 메인 video |
|---|---|---|---|
| 전 | `sound small` | 음소거(m) | `muted=false` |
| 1회 | `sound small mute` | 음소거 해제(m) | `muted=true` |
| 2회 | `sound small` | 음소거(m) | `muted=false` |

320×240짜리 두 번째 `<video>`는 항상 muted인 광고용이라 무시한다.

**찾아 고친 버그 2건**

- **CSS 특정도** — 공통 규칙 `#mlpp-chats .mlpp-audio`(1,1,0)가 개별 규칙 `#mlpp-audio-N`(1,0,0)을
  이겨 하이라이트가 영영 안 보였다. `!important`끼리는 특정도가 갈랐다.
  같은 패턴이 채팅 자리 표시자에도 있어 **그것도 한 번도 뜬 적이 없었다**(사용자가 본 "그냥 검은색"의 정체).
  공통 규칙에서 id 선택자를 빼 특정도를 낮췄다.
- **호버 감지** — `mouseenter`만 쓰면 커서가 이미 그 프레임 안에 있던 상태로 시작할 때 영영 발생하지 않는다.
  `mousemove`로도 켜고 상태가 바뀔 때만 보고하도록 고쳤다.

**검증** — `https://mul.live/marronie/ecvhao/viichan6` (SOOP 3분할).
`agents=[0,1,2]` 세 프레임 모두 핸드셰이크 성공. 가운데 클릭 → `pinned=[0] muted=[1,2]`,
해당 타일 파랑·나머지 빨강으로 표시되고 다른 두 플레이어가 실제로 음소거됐다.
페인트 순서 `mlpp-glow → streams → chat-container → mlpp-chats → chat-toggle` 확인.

### 2026-08-30 — 설정 UI를 직접 만듦

GM_config는 유저스크립트 매니저 메뉴에 얹히는 구조라 Violentmonkey 팝업이 바뀐 값을
**새로고침 전까지 보여주지 않는다**(실제 값은 바뀌어 있는데도).
그래서 값을 되돌려도 안 먹은 것처럼 보였고, 실제로 "3열 모드가 안 된다"고 본 것도
`gridCols` 를 0으로 되돌린 것이 UI에 안 보였을 뿐 저장은 되어 있던 상황이었다.
새 빌드가 저장된 진짜 값을 읽으면서 열 모드가 그대로 돌아왔다.

- `src/panel.js` 추가 — 오른쪽 위 톱니 버튼(#chat-toggle 왼쪽) + 패널.
  `settings.SCHEMA` 를 그대로 그리므로 설정을 추가하면 UI가 따라온다.
- `src/settings.js` 에서 GM_config 의존 제거. 스키마 + GM 저장소만 남았다.
- `meta.js` 에서 `@require`(GM_config)와 `GM_deleteValue` / `GM_registerMenuCommand` /
  `GM_unregisterMenuCommand` / `GM_addValueChangeListener` grant 제거. `#md5` 관리도 사라졌다.
- 메뉴 명령이던 "영상 순서 초기화" / "솔로·음소거 해제" 는 패널 버튼으로 옮겼고 "설정 초기화" 를 더했다.

**고친 버그** — 단위가 없는 숫자 항목에서 입력칸이 라벨 왼쪽에 붙었다.
`insertBefore(control, label.lastChild)` 가 단위 없을 때 이름 앞에 끼워넣은 탓.

**검증** (1920×889, SOOP 3분할, 열 모드)

| 항목 | 결과 |
|---|---|
| 톱니 버튼 | `1858,0 28x28`, `#chat-toggle`(1892,0)과 겹치지 않음 |
| 패널 | 항목 8개 + 액션 3개, 순서 `이름 \| 입력 \| 단위` |
| 즉시 반영 | 타일 간격 0 → 16 → 0 이 새로고침 없이 적용(640×360 ↔ 629×353) |
| 열 모드 | 영상 `0,0` `640,0` `1280,0` 각 640×360, 채팅 3개 |

## 남은 일

- 수동 격자를 별도 레이아웃 모드로 분리 (지금은 `gridCols > 0` 이면 열 모드가 조용히 꺼진다).
  사용자 판단으로 보류 중.

### 2026-08-30 — 사이드 채팅 빠른 전환 + 설정 탭

**사이드 채팅 빠른 전환** — 오디오 솔로와 같은 구조를 채팅에도 적용했다.
영상에 호버하면 사이드 채팅이 그 방송으로 잠깐 바뀌고(미리보기), 가운데 영역 우클릭이면 확정한다.
확정은 토글이 아니라 항상 그 방송으로 맞춘다. `layout.js` 가 `committed` 와 `preview` 를 나눠 갖는다.

**프레임 이벤트 버스** — 오디오와 레이아웃이 같은 프레임 이벤트를 쓰게 되어
`message` 리스너를 `src/frames.js` 하나로 모았다. origin 확인과 인덱스 해석도 여기서 한다.

**설정 탭** — 스키마의 `tab` 으로 묶어 패널에 탭으로 그린다.

| 탭 | 항목 |
|---|---|
| 레이아웃 | 레이아웃, 열 모드 최소 열 폭, 사이드 채팅 폭, 타일 간격, 수동 격자 열 수·행 수 |
| 채팅 | 채팅 생성 간격, 동시 유지 채팅 수 |

**검증** — 사이드 모드에서 호버 시 채팅이 따라 바뀌고, 오디오 상태의 `hovered` 값과
표시 중인 채팅 인덱스가 일치함을 확인. 패널 구조는 `H2 → 탭바 → 탭 본문 2개 → 액션바`,
탭 전환 시 본문 `display` 가 정확히 교체된다.

### 2026-08-30 — 설정 항목 정리

셋을 패널에서 뺐다. 각각 이유가 다르다.

| 항목 | 처리 | 이유 |
|---|---|---|
| 열 모드 최소 열 폭 | 상수 `400` | 기본값이 충분히 맞아 조절할 이유가 없었다 |
| 사이드 채팅 폭 | UI에서만 제거, 저장은 유지 | 리사이저 핸들이 이미 있다 |
| 타일 간격 | 상수 `0` | 붙여 놓는 편이 낫다 |

`chatWidth` 는 리사이저가 계속 써야 하므로 `HIDDEN_DEFAULTS` 로 옮겨 기본값을 살렸다.
SCHEMA에서만 빼면 `get()` 의 폴백이 0이 되어 사이드 모드가 무너진다.
"설정 초기화" 는 패널에 없는 값까지 되돌린다.

**남은 설정** — 레이아웃 탭: 레이아웃, 수동 격자 열 수·행 수 / 채팅 탭: 채팅 생성 간격, 동시 유지 채팅 수.

**검증** — 사이드 모드로 전환해 이전에 드래그로 맞춘 채팅 폭 458이 유지되는 것을 확인
(리사이저 x=1462 = 1920−458). 열 모드 복귀 후 타일 간격 0, 영상 `0` `640` `1280` 각 640폭.

### 2026-08-30 — 마스터 앤 스택

사이드 모드에서 영상 가운데를 휠클릭하면 dwm식 마스터 앤 스택으로 바뀐다.
마스터를 다시 누르면 해제, 다른 화면을 누르면 그쪽이 마스터가 된다.

스택 폭은 가용 폭의 25%를 기본으로 하되 세로로 다 들어가도록 화면 높이가 상한을 정한다.
마스터는 남은 폭을 다 쓰고, 둘 다 세로 가운데 정렬한다.

**고친 것** — `master` 처리를 채팅 사용 가능 가드보다 앞으로 옮겼다.
마스터는 영상 배치라 그 방송의 채팅을 쓸 수 있는지와 무관한데, 가드 뒤에 있으면
채팅이 없는 방송에서 휠클릭이 먹통이 된다.

**추가** — `data-mlpp-layout` 진단 속성. `mode` / `master` / 활성 채팅 / 슬롯 배열 /
수동 격자 / 설정값을 한 줄로 남긴다. 앞서 "수동 격자가 열 모드를 조용히 끄는" 상황을
눈으로 알아채지 못했던 일이 있어 넣었다.

**검증** — 1920×889, SOOP 4분할, 사이드 설정에서 실제 휠클릭:

```
mode=master master=3 chat=0 slots=[3,0,1,2] grid=0x0 setting=side
```

마스터 `0,136 1097x617`, 슬레이브 `1097` 열에 365×205 셋이 `137 / 342 / 547`.
node로 미리 계산한 값과 일치.

### 2026-08-30 — 마스터 전환이 채팅을 끌고 간다

- `masterFollowsChat` (채팅 탭, 기본 켬) — 휠클릭으로 마스터를 바꾸면 사이드 채팅도 그 방송으로 넘어간다.
  체크박스를 쓰려고 스키마에 `bool` 타입을 더했다. 값은 0/1로 저장한다.
- **호버 한 번 무시** — 슬레이브를 마스터로 올리면 그 자리에 다른 화면이 들어와 커서 밑에 놓이고,
  그 화면의 에이전트가 곧바로 호버를 보고해 채팅이 엉뚱한 곳으로 넘어갔다.
  마스터를 바꾼 직후 700ms 안에 오는 호버 하나를 소비해서 막는다. 설정 없이 항상 동작한다.
  한 번 쓰면 바로 만료되므로 그 뒤의 진짜 호버는 정상 동작한다.

**검증** — 채팅 탭에 `마스터 전환시 채팅도 전환 = 체크됨` 표시.
실제 휠클릭 후 진단이 `master=3 chat=3` (직전에는 `master=3 chat=0`).

### 2026-08-30 — 열 모드에서도 마스터 앤 스택 진입

마스터 판정을 열 모드 판정보다 앞으로 옮겼다. 이제 지금 배치가 열이든 사이드든
휠클릭이면 마스터 앤 스택으로 간다. 마스터 앤 스택은 사이드 채팅 배치라
열 모드에서 들어오면 채팅이 단일 패널로 바뀌고, 해제하면 원래 배치로 되돌아온다.
채팅 프레임은 그대로 살아 있으므로 오갈 때 리로드가 없다.

수동 격자를 지정한 상태에서는 여전히 그쪽이 우선한다.

**검증** — 자동(열 모드) 복귀 상태에서 `mode=columns master=-1`, 영상 4개 480×270 한 줄,
채팅 4개 동시 표시를 확인. 휠클릭 자체는 자동화 도구에 중간 버튼 액션이 없어 사용자 확인.

### 2026-08-30 — 솔로 하이라이트를 채팅 위로, 그리고 사운드 비주얼라이저

**층 순서 재정리** — 열 모드에서 솔로 단서가 채팅에 완전히 가려졌다.
원인은 채팅 iframe이 `z-index: 2`, 영상이 `auto(0)` 이라 **채팅이 영상보다 위**에 있었던 것.
글로우를 "채팅 위, 영상 아래"에 두려면 층을 다시 쌓아야 했다.

| 층 | z-index |
|---|---|
| 채팅 iframe | 1 |
| 자리 표시자 | 2 |
| 솔로 글로우 | 3 |
| 영상 iframe | 4 |
| 리사이저 | 5 |
| `#chat-select` | 6 |
| 드래그 타일 | 7 |

**일렁임** (`glowPulse`, 기본 켬) — `@keyframes` 로 box-shadow 를 왕복시킨다.
CSS 애니메이션이라 프레임마다 드는 비용이 없다. 타일마다 `animation-delay` 를 어긋나게 줘
한 덩어리로 깜빡이지 않게 했다.

**실제 소리 반영** (`glowFromAudio`, 기본 끔) — 프레임 안 에이전트가 `<video>` 에
`AnalyserNode` 를 걸어 초당 약 16번 크기를 보고하고, 부모는 인라인 box-shadow 만 갱신한다
(전체를 다시 그리지 않는다).

기본을 끔으로 둔 이유: `createMediaElementSource` 를 부르면 그 요소의 오디오가 영영
Web Audio 그래프를 통과하게 된다. `destination` 까지 잇지 않으면 소리가 끊기고, 되돌릴 수 없다.
그래서 끌 때도 그래프는 그대로 두고 측정 루프만 멈춘다.

**검증** — 사운드 탭 생성(항목 2개)과 z 순서를 실측으로 확인.
일렁임과 실제 소리 반영은 눈·귀로 봐야 하므로 사용자 확인이 필요하다.

### 2026-08-30 — 파동 형태로 변경

상시 맥동에서 **peak 순간에 선이 밖으로 퍼져나가는 파동**으로 바꿨다.
바탕 글로우와 box-shadow 가 서로 덮어쓰지 않도록 파동은 별도 자식(`.mlpp-ripple`)이 맡고,
Web Animations 로 한 발씩 재생한다. 전체를 다시 그리지 않는다.

**peak 검출** — 절대 임계값을 쓰면 조용한 방송은 영영 안 뛰고 시끄러운 방송은 계속 뛴다.
지수이동평균(감쇠 0.9)으로 기준선을 따라가고, 순간 크기가 `기준선 × 1.35 + 0.06` 을 넘을 때만
쏜다. 불응기 140ms.

배수는 시뮬레이션으로 정했다(12초 기준 발수):

| 배수 | 무음 | 일정 소음 | 조용한 방송+말소리 | 시끄러운 방송+말소리 | 음악 |
|---|---|---|---|---|---|
| 1.5 | 0 | 3 | 17 | **4** | 50 |
| **1.35** | 0 | 3 | 17 | **19** | 50 |
| 1.25 | 0 | 3 | 17 | 19 | 50 |

1.5는 이미 시끄러운 방송에서 기준선이 높아져 말소리를 거의 못 잡았다.
1.35에서 조용한/시끄러운 방송이 비슷해지고 무음은 여전히 0이다. 1.25와 같은 결과라 여유를 두고 1.35로 뒀다.
"일정 소음"의 3발은 기준선이 잡히는 초반에만 나온다.

**실제 소리를 안 쓸 때** — 부모가 1.5초 주기로, 타일마다 170ms 씩 어긋나게 파동을 보낸다.

### 2026-08-30 — 하이라이트 상시 표시, 그리고 파동이 안 보이던 버그

**상시 표시** — z 층을 제대로 쌓았으니 호버 게이팅을 걷어냈다.
이제 하이라이트는 **들리는 화면에만** 붙고 계속 보인다. 기본 상태와 음소거 화면에는 아무것도 없다.
표시가 있으면 곧 "들린다"는 뜻이라 규칙이 단순해졌다.

**파동이 안 보이던 원인** — `.mlpp-ripple` 에 `opacity: 0 !important` 를 걸어둔 것.
CSS 캐스케이드에서 **important 선언은 애니메이션보다 우선**이라,
Web Animations 가 올린 opacity 가 계속 0으로 눌려 영영 보이지 않았다.
`!important` 를 떼서 고쳤다. 우리가 만든 요소라 페이지 스타일과 겨룰 일이 없어 붙일 이유도 없었다.

거기에 파동은 `shown` 목록의 타일에만 쏘는데 그 목록이 호버 중에만 채워졌으니 이중으로 막혀 있었다.
상시 표시로 바꾸면서 함께 풀렸다.

**보이게 다듬은 것** — 파동 색을 바탕(alpha 0.5)보다 진한 alpha 0.95 로,
지속 620ms → 900ms, 최대 확산 32px → 42px, 중간 지점(35%)을 넣어 선이 퍼지는 느낌을 살렸다.

**검증** — `.mlpp-ripple` 에 시험 애니메이션을 걸어 computed opacity 가 1까지 오르는 것을 확인.
솔로 상태에서 `mlpp-audio-3` 만 `block`, 음소거된 0·1·2 는 `none`.

### 2026-08-30 — 파동을 선으로, 고정 하이라이트 제거

**선으로** — `box-shadow` 의 spread 는 면을 채우기 때문에 "사각형이 커지는" 모양이 된다.
`outline` + `outline-offset` 으로 바꿨다. outline 은 두께가 그대로인 선이라
offset 만 키우면 선 하나가 그대로 밖으로 이동한다. 레이아웃에도 영향을 주지 않는다.

**고정 하이라이트 제거** — `.mlpp-audio` 의 바탕 `box-shadow` 를 없앴다. 단서는 지나가는 선뿐이다.

**불투명도** — 최대 0.6 으로 낮췄다.

**검증** — `.mlpp-audio` 의 `box-shadow` 가 `none`,
파동 키프레임을 걸었을 때 `outline-offset` 이 0 → 15px(1200ms 중 500ms 지점, linear)로 움직이고
opacity 가 0.358 로 내려가는 것을 확인.

### 2026-08-30 — 파동 속도·거리 조정

이동 거리 38px → 15px(2/5), 지속 900ms → 1300ms.
겉보기 속도는 원래의 약 27%가 된다.

### 2026-08-30 — 파동을 제자리에 고정

선이 밖으로 이동하지 않고 테두리에 붙어 밝기만 오르내리게 바꿨다.
주기(1.5초)와 지속(1300ms)은 그대로다. 세기는 이동 거리 대신 최고 불투명도로 나타낸다.

### 2026-08-30 — 마스터 전환시 사운드도 전환

`masterFollowsAudio` (사운드 탭, 기본 켬). 마스터가 된 방송은 솔로에 들어가고,
마스터를 풀면 원래 솔로였던 것만 남는다.

되돌리려면 "우리가 넣은 것인지"를 기억해야 한다. `masterAutoPinned` 하나로 추적한다.
마스터인 동안 그 화면을 직접 솔로 토글하면 소유권이 사용자에게 넘어가 되돌리지 않는다.

**검증** — 규칙을 그대로 옮겨 시뮬레이션:

| 상황 | 결과 |
|---|---|
| 솔로 없음 → 마스터2 → 해제 | `{2}` → `{}` |
| 솔로 `{1}` → 마스터1 → 해제 | `{1}` → `{1}` |
| 솔로 `{1}` → 마스터2 → 마스터3 → 해제 | `{1,2}` → `{1,3}` → `{1}` |
| 마스터2 후 직접 솔로 클릭 → 해제 | `{2}` (사용자 조작 존중) |

`glowPulse` 설명이 예전 "일렁임" 문구로 남아 있어 현재 동작(테두리가 밝아졌다 사라짐)에 맞게 고쳤다.

### 2026-08-30 — 솔로 하이라이트가 사라지던 버그, 그리고 패널 안내문

**하이라이트가 안 나오던 원인** — `실제 소리에 반응` 을 켜면 주기 파동 타이머를 꺼버리고
소리 beat 에만 의존했다. 분석이 실패하면(플레이어가 이미 오디오 소스를 잡았거나 재생 전이라
video 크기가 0인 경우) 파동이 하나도 안 나오고, 고정 표시를 없앤 뒤라 **솔로 표시가 통째로 사라졌다.**
실측: 3.2초 동안 ripple 애니메이션 0개, `실제소리반응=켬`.

고친 방향 — 주기 파동을 **바닥으로 항상 돌린다.** 최근에 소리로 쏜 타일만 건너뛴다.
분석이 되든 안 되든 표시는 남는다.

곁들여:
- 에이전트가 `analyser` 로 성공 여부를 보고하고 `data-mlpp-audio` 에 `analysers=[...]` 로 남는다.
- `mainVideo()` 를 임계값(>400) 대신 **가장 큰 video** 선택으로 바꿨다. 재생 전에는 모두 0이라 임계값이 무의미했다.
- 재생 전이면 `playing` 이벤트에서 한 번 더 시도한다.

**검증** — 고친 뒤 4초에 파동 9회, `analysers=[0,1,2,3]` 네 프레임 모두 분석 성공.

**패널 안내문** — 제목을 "Mul.Live++ 관리 패널" 로 바꾸고 탭마다 맨 위에 한 줄 안내를 넣었다.
호버 동작은 일부러 적지 않는다.
