# mullive-plusplus SPEC

Mul.Live(https://mul.live)에 얹는 유저스크립트. 사이트를 포크하지 않고 기존 페이지를 강화한다.

## 대상 / 스코프

- `@match https://mul.live/*`, `https://www.mul.live/*`
- 스트림이 없는 랜딩 페이지에서는 훅을 못 찾으므로 자동 no-op.
- **SOOP 인스턴스 전용으로 개발·검증한다.** chzzk/트위치/유튜브는 대상이 아니며 플랫폼별 특수 처리를 넣지 않는다.
  (채팅 멀티플렉서 등은 플랫폼 구분 없는 코드가 더 단순하므로 결과적으로 함께 동작할 수 있으나, 검증하지 않는다.)
- SOOP 채팅은 Mul.Live Plus 확장 프로그램이 있어야 활성화된다. 확장 미설치 시 해당 옵션은 `disabled`이며 스크립트는 이를 건너뛴다.

## 업스트림 구조

`jebibot/mullive`는 Cloudflare Worker 단일 파일(`src/index.ts`)이 HTML 전체를 문자열로 생성한다.
프레임워크·번들러·클래스명 없음. 아래 id들이 훅의 전부다.

## 페이지 훅

| 셀렉터 | 내용 |
|---|---|
| `#streams` | 영상 컨테이너. `display:flex; flex-wrap:wrap` |
| `#streams iframe` | 영상 iframe. `name` = 스트림 id (숫자 id면 `#id`) |
| `#chat-container` | 채팅 패널. **`width: 350px` 하드코딩** |
| `#chat-select` | 채팅 선택 `<select>`. `option[value]` = 채팅 URL, 마지막 항목은 `about:blank`(숨기기) |
| `#chat-select option[disabled]` | 확장 프로그램 필요 (SOOP) |
| `#chat` | 채팅 iframe **1개**. `src` 교체로 채널 전환 → 전환할 때마다 전체 리로드 |
| `#chat-toggle` | 채팅 열기/닫기. `chat.src`를 `about:blank`와 토글 |

페이지 전역 스크립트가 하는 일:

- `adjustLayout()` — `resize`와 `#chat`의 `load`에서 호출. cols 1..n을 순회해 16:9 최대 타일을 찾고 iframe에 **인라인** `style.width/height`를 쓴다. 가용 폭 계산에 **350을 하드코딩**한다.
- `setName(i, name)` — 스트리머 이름을 `#chat-select`의 옵션 텍스트에 반영.
- `message` 리스너 — `https://play.sooplive.com` 출처의 `PonReady` / `PupdateBroadInfo` / `showRefreshOverlay` 처리.
- `securitypolicyviolation` 리스너 — `https://nid.naver.com` 차단 시 네이버 로그인 창을 연다.

## 제약 (설계를 강제하는 사실)

1. **CSP**: `script-src 'nonce-…'; style-src 'nonce-…'`, `unsafe-inline` 없음. HTTP 헤더로만 전달된다(meta 태그 없음).
   → `@grant none`이면 주입 자체가 차단된다. GM API를 최소 1개 grant해 샌드박스 컨텍스트로 실행해야 한다.
   → 스타일 주입은 `src/style.js`의 3단 폴백(GM_addStyle → adoptedStyleSheets → `<style>`)을 통한다.

   **2026-08-29 실측** (페이지 컨텍스트에서 직접 확인):
   - `<style>` 요소 삽입 → **차단됨**. `style-src-elem` 위반 이벤트 발생, 스타일 미적용.
   - `document.adoptedStyleSheets` + `CSSStyleSheet.replaceSync` → **통과**. 위반 없이 정상 적용.

   CSP에 막힌 `<style>` 요소도 DOM에 남아 `isConnected === true`이므로, 주입 성공 여부는
   **요소의 존재가 아니라 효과로 판정해야 한다.** `style.js`는 센티넬 커스텀 속성(`--mlp-style-ok`)이
   계산된 스타일에 반영됐는지로 각 단계를 검증한다.
2. **iframe을 DOM에서 옮기면 리로드된다.** 타일 위치 교환은 DOM 이동 없이 CSS 좌표만 바꾼다.
3. 페이지 `adjustLayout()`은 **인라인 style**을 쓴다. 우리 규칙은 전부 `!important`로 준다. 메인 월드 접근이나 함수 패치는 불필요.
4. 새 iframe은 페이지 `frame-src` 화이트리스트 안이어야 한다.
   허용: `'self'`, `chzzk.naver.com`, `*.chzzk.naver.com`, `*.twitch.tv`, `*.sooplive.com`, `www.youtube.com`.
   `#chat-select`의 모든 채팅 URL이 이 안에 들어가므로 채팅 iframe 추가 생성은 가능하다.
5. **SOOP 채팅 프레임은 자기 플레이어가 준비된 뒤에만 방에 입장할 수 있다.**
   확장(`soop.js`)이 채팅 프레임에서 `window.opener = window.parent[플레이어이름]`으로 플레이어를 이어주고,
   채팅은 그 플레이어에게서 방송 정보를 받아 입장한다. 플레이어가 준비되기 전에 채팅 프레임을 만들면
   UI 껍데기만 뜨고 영영 입장하지 못한다. 페이지 원본이 `PonReady`를 받은 뒤에야 `#chat`의 src를 채우는 이유다.
   → `src/ready.js`가 `PupdateBroadInfo` 또는 `PonReady` + 0.5초를 기다린 뒤 채팅을 만든다.
   SOOP 외 플랫폼의 채팅은 플레이어와 무관하므로 기다리지 않는다.
   `PonReady` 즉시(약 700ms)에 만들면 3개 중 1개만 입장한다. 페이지가 그 신호에 대한 응답으로
   보내는 `Pload`를 플레이어가 처리할 시간이 필요하다.
7. **채팅을 한꺼번에 띄우면 실패한다.** 플레이어들이 동시에 재생을 시작하는 시점과 겹친다.
   → 생성 큐를 두고 `chatStagger`(기본 800ms) 간격으로 하나씩 만든다.
8. **교차 출처 프레임의 window는 `instanceof Window`가 false다.** 프로토타입 체인이 막혀 있다.
   `===` 비교와 `Set` 멤버십은 정상이므로 프레임 식별은 그쪽으로 한다.
9. **SOOP 플레이어의 postMessage API에는 음소거 명령이 없다.**
   외부 명령은 `Pload` `Pplay` `Ppause` `PtoggleChat` `PsetDarkMode` `PisPlayerWatching` 뿐이다.
   디스패치는 화이트리스트가 아니지만(`t.data.cmd && "function"==typeof this[t.data.cmd]`)
   그 클래스에 음소거 메서드 자체가 없다.
   → **유저스크립트를 플레이어 iframe 안에서도 돌린다.** 프레임 안에서 `#btn_sound`를 직접 누른다.
   버튼은 토글이지만 `mute` 클래스로 현재 상태를 읽을 수 있어 절대 상태를 맞출 수 있다.
10. **부모는 타일 위 호버를 알 수 없다.** iframe이 포인터 이벤트를 삼킨다.
   → 프레임 안의 에이전트가 호버·가운데 클릭을 부모에 보고한다. 플레이어 조작을 전혀 막지 않는다.
11. 페이지 리스너를 떼어낼 수 없다(함수 참조 접근 불가). `document`의 **캡처 단계**에서 `stopPropagation()`으로 선점한다.
   페이지 JS가 `#chat` 등의 참조를 들고 있으므로 해당 요소는 DOM에 남겨둔다.

## 레이아웃 모델

배치는 두 모드를 오간다. `auto`가 기본이며 화면 모양과 방송 수로 자동 선택한다.

### columns — 영상 1행, 각 영상 아래 자기 채팅

n개 열을 만들고 열마다 위에 영상, 아래에 그 방송의 채팅을 둔다. **모든 채팅이 동시에 보인다.**
열 폭이 곧 영상 폭이자 채팅 폭이고, 영상은 16:9로 열 폭을 꽉 채우므로 채팅 높이는 자동으로 정해진다.
조절할 자유도가 없다.

발동 조건(둘 다 만족):

- **가로 화면**(`뷰포트 폭 >= 높이`). 세로 화면에서 열을 나누면 채팅이 비정상적으로 길어진다.
- **열 폭 >= `minColumnWidth`**(기본 400px).

| 화면 | 방송 수 | 결과 |
|---|---|---|
| 1920×1080 | 3 | 열 모드 — 영상 637×358 / 채팅 637×718 |
| 1920×1080 | 4 | 열 모드 — 영상 477×268 / 채팅 477×808 |
| 1920×1080 | 5 이상 | 열 폭 381 → 사이드 |
| 1366×768 | 3 | 열 모드 — 영상 452×254 / 채팅 452×510 |
| 2560×1440 | 4 | 열 모드 — 영상 637×358 / 채팅 637×1078 |
| 1080×1920 (세로) | 3 | 세로 화면 → 사이드 (영상 3×1) |

### side — 영상 격자 + 오른쪽 단일 채팅

영상은 16:9 타일을 가장 크게 만드는 격자에 놓고, 마지막 행에 남는 타일은 그 행 안에서 가운데 정렬한다.
채팅은 오른쪽 패널 하나만 보이고 나머지는 뒤에서 살아 있는다. 패널 폭은 리사이저로 조절한다.

리사이저는 **레이아웃 공간을 전혀 차지하지 않는다.** 채팅 왼쪽 여백 위에 겹쳐 놓아
영상 영역과 채팅이 딱 붙는다. 세로 스트립을 따로 떼어주면 그만큼 화면이 버려진다.
세로 전체가 잡는 영역이지만 **보이는 것은 가운데의 짧은 그립뿐**이다(4×56px, 흰색 10% 불투명).
올리면 80px에 40%로 진해진다. `#chat-select`는 그 폭만큼 오른쪽으로 밀어 잡는 영역을 가리지 않게 한다.

### master — 마스터 앤 스택

사이드 모드의 변형이다. 큰 화면 하나를 두고 나머지를 한 줄로 쌓는다.
채팅은 사이드와 같이 오른쪽 패널 하나다.

- 영상 가운데 영역 **휠클릭**으로 조작한다. 마스터를 다시 누르면 해제, 다른 화면을 누르면 그쪽이 마스터가 된다.
- 스택 위치는 `masterStackPlacement`로 고른다.
  - `bottom`(기본) — 마스터가 위, 스택이 아래 가로줄. 스택 높이는 화면 높이의 25%를 기본으로 하되,
    가로로 다 들어가도록 가용 폭이 상한을 정한다.
  - `right` — 마스터가 왼쪽, 스택이 오른쪽 세로줄. 스택 폭은 가용 폭의 25%가 기본이고 화면 높이가 상한이다.
- 어느 쪽이든 **스택이 먼저 자기 크기를 정하고 마스터가 남은 자리를 16:9로 채운다.**
- **가로 화면에서는 `bottom` 이 크게 유리하다.** 스택이 세로줄이면 화면 높이가 슬레이브 크기를 조이고
  남는 폭을 마스터가 다 쓰지 못한다. 1920×1080 · 채팅 350 · 4방송 실측:

  | 스택 위치 | 마스터 | 영역 커버 |
  |---|---|---|
  | `bottom` | 1440×810 | 91.7% |
  | `right` | 1178×662 | 61.3% |

  이것이 기본값을 `bottom` 으로 두는 근거다. 세로 화면에서는 둘 다 나쁘다(28.9% / 13.0%).
- 마스터는 영상 배치라 그 방송의 채팅을 쓸 수 있는지와 무관하다.
- 열 모드에서도 들어갈 수 있다. 마스터 앤 스택은 사이드 채팅 배치라, 열 모드에서 들어오면
  채팅이 단일 패널로 바뀐다. 마스터를 다시 눌러 해제하면 원래 배치로 되돌아온다.
- 수동 격자를 지정한 상태에서는 그쪽이 우선한다. 새로고침하면 풀린다(저장하지 않는다).
- 마스터를 바꾸면 사이드 채팅도 그 방송으로 넘어간다(`masterFollowsChat`, 기본 켬).
- 마스터를 바꾸면 솔로도 따라간다(`masterFollowsAudio`, 기본 켬).
  마스터가 된 방송은 솔로에 들어가고, 마스터를 풀면 **원래 솔로였던 것만 남는다**.
  우리가 넣은 것인지 사용자가 넣은 것인지를 기억해 되돌린다.
  마스터인 동안 그 화면을 직접 솔로 토글하면 그때부터는 사용자 것으로 보고 되돌리지 않는다.
- 슬레이브를 마스터로 올리면 그 자리에 다른 화면이 들어와 커서 밑에 놓인다.
  그때 곧바로 오는 **호버 한 번은 무시**해서 방금 고른 마스터의 채팅이 그대로 보이게 한다.
  설정 없이 항상 그렇게 동작한다.

1920×889, 채팅 458 기준:

| 방송 수 | 마스터 | 슬레이브 |
|---|---|---|
| 3 | 1097×617 | 365×205 ×2 |
| 4 | 1097×617 | 365×205 ×3 |
| 6 | 1146×644 | 316×177 ×5 |

### 수동 격자

`gridCols`를 지정하면 그 열 수로 고정한다. `gridRows`로 최소 행 수를 잡으면 빈 칸이 남는다.
방송 수에 필요한 행보다 적으면 필요한 만큼 늘어난다.
수동 격자에서는 영상이 여러 행에 놓이므로 "영상 아래 자기 채팅"이 성립하지 않는다. 사이드 채팅이 된다.

## 기능

### F1. 채팅창 너비 조절 (사이드 모드)
- 영상과 채팅 패널 사이의 리사이저를 끌어 조절. 더블클릭 시 350px.
- 범위 `[240px, 뷰포트 폭 * 0.6]`. 저장값은 창 크기 때문에 덮어쓰지 않는다.

### F2. 채팅 백그라운드 유지
- 한 방송당 채팅 iframe 하나. 한 번 만들면 계속 살려두므로 전환해도 다시 로드되지 않는다.
- 안 보이는 채팅은 크기를 유지한 채 `visibility`로만 감춘다. `display:none`으로 접으면 크기가 0이 되어 채팅이 내려가지 않는다.
- 열 모드에서는 전부 보이므로 전환 자체가 없다.
- 확장이 없어 `disabled`인 채팅은 만들지 않는다.
- SOOP 채팅은 플레이어 준비 신호를 기다렸다 만든다(제약 5).
- **유지 상한**(`chatLimit`, 기본 0 = 무제한): 살아 있는 채팅 수가 상한을 넘으면 오래 안 본 것부터
  `display:none`으로 렌더만 멈춘다. 문서와 연결은 남으므로 다시 열면 즉시 보인다.
  보이는 채팅은 절대 대상이 되지 않는다.

### F3. 절대 위치 배치
- 영상·채팅 iframe을 전부 절대 위치로 놓는다. DOM 구조를 바꾸지 않으므로 배치를 바꿔도 재생이 끊기지 않는다.
- 페이지의 `#chat`은 쓰지 않고 `about:blank`로 고정한다. 확장이 `PonReady`에서 src를 채우므로 계속 되돌린다.
- 페이지의 `#chat-select`는 우리 컨테이너로 옮겨 재사용한다(iframe이 아니라 옮겨도 리로드되지 않는다).

### F4. 솔로 / 음소거

DAW·오인페의 솔로처럼 동작한다. **들리는 것 = 고정 ∪ 호버**.

- 아무것도 없으면 전부 들린다. 하이라이트도 없다.
- 화면에 호버하면 그 화면만 들린다(나머지 자동 음소거).
- 화면 **가운데 영역 좌클릭**으로 고정 토글. 고정은 여러 개 가능하다 — 여러 방송을 동시에 듣는다.
- 하이라이트는 **들리는 화면에만** 나타난다. 기본 상태(모두 들림)와 음소거된 화면에는 아무것도 그리지 않는다
  — 표시가 있으면 곧 "들린다"는 뜻이다.
- 상시 표시는 두지 않는다. 단서는 **테두리 선이 밝아졌다 사라지는 것**뿐이다. 선은 움직이지 않는다.
  `outline` 으로 그린다. `box-shadow` 의 spread 는 면을 채워 사각형처럼 보이는 반면
  `outline` 은 두께가 일정한 선이고 레이아웃에도 영향을 주지 않는다.
- 글로우는 **채팅 위, 영상 아래**에 그린다. 영상 아래여야 이웃 화면을 가리지 않고,
  채팅 위여야 열 모드에서 채팅에 완전히 가려지지 않는다. 타일 사이에 여백은 만들지 않는다.
  층 순서: 채팅(1) < 자리표시자(2) < 글로우(3) < 영상(4) < 리사이저(5) < select(6) < 드래그 타일(7).
- 파동은 `glowPulse`(기본 켬)로 켜고 끈다. 테두리 선이 밝아졌다 1300ms에 걸쳐 사라진다(최대 불투명도 0.6, 세기에 따라 가감).
- `glowFromAudio`(기본 켬)를 켜면 **실제 소리의 국소 peak 마다** 한 발 쏜다.
  프레임 안 에이전트가 `<video>`에 `AnalyserNode`를 걸고, 지수이동평균으로 기준선을 따라가다
  순간 크기가 기준선의 1.35배 + 0.06 을 넘으면 보고한다(불응기 140ms).
  절대 임계값을 쓰면 조용한 방송은 영영 안 뛰고 시끄러운 방송은 계속 뛴다. 기준선이 방송마다 알아서 맞는다.
- 주기 파동(1.5초, 타일마다 170ms 씩 어긋나게)은 **항상 바닥으로 돈다.** 실제 소리로 최근에 쏜 타일만 건너뛴다.
  실제 소리 쪽이 실패해도(플레이어가 이미 오디오 소스를 잡았거나 재생 전이라 `videoWidth`가 0) 표시는 남는다.
- **`AudioContext`가 멈춘 채로 만들어지면 그 방송은 통째로 무음이 된다.**
  `createMediaElementSource` 가 오디오를 그래프로 돌려버리는데 되돌릴 수 없기 때문이다.
  에이전트가 `pointerdown`·`keydown` 마다 `resume()` 하고, `statechange` 로 상태를 부모에 알린다.
  `data-mlpp-audio` 의 `analysers` 는 **`state === 'running'` 인 것만** 센다.
- 비용은 실측으로 무시할 수준이다. `fftSize` 256 + 256칸 피크 루프가 1회 **0.0015ms**,
  4방송 60fps 기준 코어 1개의 **0.037%**. 측정 중 부모 탭 프레임 간격은 중앙값 16.6ms / p95 17.0ms.
  기본을 켬으로 두는 근거다.

### F5. 사이드 채팅 전환과 분할

사이드 모드와 마스터 앤 스택에서만 의미가 있다. 열 모드는 채팅이 전부 보이므로 해당 없다.

영상 가운데 영역 **우클릭**이 하는 일은 `chatRightClick` 으로 고른다.

- `채팅 전환`(기본) — 항상 그 방송으로 맞춘다. 토글이 아니다.
- `채팅 추가/제거` — 채팅 패널을 쪼개 여러 방송을 같이 본다. 이미 있는 방송을 우클릭하면 도로 빠진다.
  마지막 칸을 빼면 채팅 패널 자체를 접는다(타일링 WM 에서 마지막 창을 닫는 것과 같다).
  접힌 상태에서 우클릭하면 다시 편다. 자리가 없으면 조용히 무시한다.

**호버는 설정과 무관하게 언제나 마지막 칸만 바꾼다.** 첫 칸(메인)은 건드리지 않고 칸 수도 그대로다.
잠깐 넘겨보는 동안 배치가 흔들리면 그게 더 방해가 된다. 마우스를 떼면 원래 채팅으로 돌아온다.
`chatHoverPreview`(기본 켬)를 끄면 호버는 채팅에 관여하지 않는다.

**마스터를 바꾸면 그 방송의 채팅이 첫 칸에 놓인다**(`masterFollowsChat`, 기본 켬).
자리가 없으면 칸을 늘리는 대신 맨 뒤를 밀어낸다.

미리보기 중에도 `#chat-select` 드롭다운은 첫 칸을 가리킨다.
드롭다운으로 고르면 쪼갠 것이 한 칸으로 돌아간다 — 페이지의 그 컨트롤은 "이 채팅을 봐라"는 뜻이다.

#### 칸 나누기

`splitChatPanes`. 타일링 WM 의 재귀 이분할을 쓰되 세 군데를 채팅에 맞게 바꿨다.

1. **반씩이 아니라 담을 칸 수에 비례해서** 자른다. BSP처럼 절반씩 자르면 3칸일 때 1:1:2 로 삐뚤어진다.
2. **긴 변이 아니라 세로선(열)을 먼저** 자른다. "긴 변을 자른다"는 정사각형에 가까운 칸을 만드는 규칙이라
   창에는 맞지만 채팅에는 맞지 않는다. 채팅은 지나간 줄이 몇 개 보이느냐가 전부라 **높이가 곧 쓸모**다.
   폭은 최소치까지 깎아도 읽히지만 높이는 아니다. 그래서 폭을 먼저 쓰고, 열이 최소 폭(240)에
   걸려 더 못 쪼개질 때만 가로로 눕힌다.
3. **첫 칸은 메인으로 떼어 두고 나머지끼리만 나눈다.** 칸이 늘 때마다 전부 다시 비례 배분하면
   처음 보던 채팅이 계속 쪼그라든다. 한 번만 갈라 메인에 절반을 주고, 새 칸들은 남은 절반 안에서만
   자리를 나눈다. dwm 의 master + stack 과 같은 구조다. 다만 stack 쪽은 1번 규칙대로 고르게 나눈다 —
   채팅끼리는 대등해서 큰 놈 하나만 남길 이유가 없다.

메인 몫을 지키면 다 못 담는 좁은 패널에서는 메인을 포기하고 전부 고르게 나눈다.
메인을 지키려다 칸을 못 늘리는 것보다 낫다. 한 칸의 하한은 240×200이고, 한 칸뿐일 때는 따지지 않는다.

1920×889 실측:

| 채팅 패널 | 2칸 | 3칸 | 4칸 | 5칸 |
|---|---|---|---|---|
| 745 (넓힘) | 373×889 + 372×889 | 373×889 + 372×445·444 | 373×889 + 372×297·296 | 373×889 + 372×223·222 |
| 350 (기본) | 350×445 + 350×444 | 350×445 + 350×222 | 350×223 ×4 (메인 포기) | 안 들어감 |

### F6. 드래그 위치 교환
- **Alt(맥에서는 Option)를 누른 동안만** 각 영상 위에 오버레이가 나타나고, 끌어서 두 타일의 위치를 맞바꾼다.
  평소 오버레이는 `pointer-events: none`이라 플레이어 조작을 전혀 방해하지 않는다.
  상시 핸들을 얹으면 SOOP 플레이어의 자체 UI를 가리고 클릭을 먹는다.
- 바뀌는 것은 **슬롯(화면상의 자리) → 스트림(방송) 대응**뿐이다. DOM은 건드리지 않으므로 재생이 끊기지 않는다.
- 열 모드에서는 채팅이 자기 영상을 따라간다.
- 드래그 중에는 화면 전체를 덮는 실드를 깔아 iframe이 포인터 이벤트를 삼키지 않게 한다.
- 순서는 `location.pathname` 단위로 저장한다. 방송 구성이 바뀌면 기본 순서로 돌아간다.
- 메뉴 명령 "영상 순서 초기화"로 되돌릴 수 있다.

## 설정 스키마

오른쪽 위 톱니 버튼을 누르면 열리는 **Mul.Live++ 관리 패널**에서 바꾼다. 값을 바꾸면 즉시 화면에 반영된다.
항목은 `tab` 으로 묶여 패널에 탭으로 나온다. 스키마에 적힌 탭 순서를 그대로 따르므로
설정을 추가하면 UI가 알아서 따라온다.

탭마다 맨 위에 한 줄 안내(`TAB_HINTS`)를 둔다. **클릭 조작만 적는다.**
호버는 바로 아래 "호버로 미리 확인" 토글이 대신 알려주므로 안내문에서는 뺀다.
`{ label, text }` 로 두고 앞머리를 굵게 그린다.

| 탭 | 앞머리 | 안내 |
|---|---|---|
| 레이아웃 | 마스터 지정 | 휠클릭으로 한 플레이어를 확대하세요. |
| 채팅 | 채팅 전환 | 플레이어에 우클릭해서 채팅을 전환/추가하세요. |
| 사운드 | 솔로 지정 | 플레이어에 좌클릭해서 듣고 싶은 영상들을 지정할 수 있어요. |

항목을 꾸미는 두 가지:

- `indent: true` — 바로 위 항목에 딸린 하위 설정으로 들여쓰고 왼쪽에 세로줄을 긋는다.
- `inline: true` — enum을 아랫줄이 아니라 이름 오른쪽에 붙인다(`라벨 [드롭다운]`).
- `group: '<이름>'` — 같은 값을 가진 이웃 항목끼리 위아래 선으로 묶고 **가로로** 늘어놓는다.
  설명은 항목마다 달지 않고 `GROUP_HELP` 로 상자 아래 한 번만 단다. 가로로 좁아 읽기 어렵기 때문이다.

GM_config를 쓰지 않는다. 유저스크립트 매니저 메뉴에 얹히는 구조라 Violentmonkey 팝업이
바뀐 값을 새로고침 전까지 보여주지 않는다(실제 값은 바뀌어 있는데도).

| 탭 | 키 | 타입 | 기본값 | 설명 |
|---|---|---|---|---|
| 레이아웃 | `layoutMode` | enum | `자동` | 자동 / 열 / 사이드. 열을 고르면 발동 조건을 무시하고 강제한다 |
| 레이아웃 | `gridCols` | int | 0 | 수동 격자 열 수 (0 = 자동). `수동 격자` 상자로 묶인다 |
| 레이아웃 | `gridRows` | int | 0 | 수동 격자 최소 행 수 (0 = 자동). `수동 격자` 상자로 묶인다 |
| 레이아웃 | `masterStackPlacement` | enum | `스택은 마스터 아래` | 마스터 앤 스택에서 스택을 아래에 깔지 우측에 세울지 |
| 사운드 | `audioHoverPreview` | bool | 켬 | 호버한 방송을 잠깐 들려준다. 끄면 고정된 조합만 들린다 |
| 사운드 | `masterFollowsAudio` | bool | 켬 | 마스터를 바꾸면 솔로도 따라간다 |
| 사운드 | `glowPulse` | bool | 켬 | 선택된 영상 시각화. 테두리가 깜빡인다 |
| 사운드 | `glowFromAudio` | bool | 켬 | 깜빡임을 실제 소리에 맞춘다 (`glowPulse` 하위) |
| 채팅 | `chatHoverPreview` | bool | 켬 | 호버한 방송으로 **마지막 칸**을 잠깐 바꾼다 |
| 채팅 | `masterFollowsChat` | bool | 켬 | 마스터를 바꾸면 그 채팅이 **첫 칸**에 온다 |
| 채팅 | `chatRightClick` | enum | `채팅 전환` | 우클릭이 채팅을 바꿀지, 칸을 넣고 뺄지 |
| 채팅 | `chatStagger` | int | 800 | 채팅을 하나씩 만드는 간격(ms) |
| 채팅 | `chatLimit` | int | 0 | 동시 유지 채팅 수. 0이면 무제한 |

패널에 노출하지 않는 값:

- `chatWidth` — 리사이저 핸들로 조절하므로 숫자 입력칸이 따로 필요 없다. 저장은 된다.
- `order:<pathname>` — 드래그 교환 순서.

상수로 고정한 값:

- 열 모드 최소 열 폭 `400px` — 기본값이 충분히 맞아 조절할 이유가 없었다.
- 타일 간격 `0` — 붙여 놓는 편이 낫다고 판단했다.

## 스코프 제외

- 1대형 + N소형 포커스 배치, PiP
- 열 모드에서 영상/채팅 높이 비율 조절 — 영상이 열 폭을 꽉 채우므로 자유도가 없다
- 채팅 필터/알림/통합 채팅
- 스트림 추가·제거 UI (URL 편집은 사이트 기능)
- chzzk 네이버 로그인 흐름 등 SOOP 외 플랫폼 특수 처리
