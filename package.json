{
  "name": "mullive-plus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mul.Live 멀티뷰 강화 유저스크립트",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc -p jsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/tampermonkey": "^5.5.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
